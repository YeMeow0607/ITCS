git clone url
git status (finding a chnages file)
git add* (*=all)
git commit -m "comment"
git config --global user.name "username"
git config --global user.email "user email"
git push -u origin main (main= branch name)